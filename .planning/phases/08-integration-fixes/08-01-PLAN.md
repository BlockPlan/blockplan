---
phase: 08-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/plan/_components/CalendarView.tsx
  - app/syllabi/upload/page.tsx
  - app/plan/_components/PlanGrid.tsx
  - app/plan/_components/DayTimeline.tsx
  - app/plan/actions.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VIEW-01, STDY-02]
gap_closure: true

must_haves:
  truths:
    - "CalendarView DayView shows Study link on exam and reading tasks"
    - "Upload page has shared NavHeader with navigation to dashboard, plan, tasks, courses, settings"
    - "PlanGrid.tsx and DayTimeline.tsx are deleted from the codebase"
    - "REQUIREMENTS.md checkboxes and traceability table reflect actual completion status"
  artifacts:
    - path: "app/plan/_components/CalendarView.tsx"
      provides: "Study session entry points in DayView for exam/reading tasks"
      contains: "/study?task_id="
    - path: "app/syllabi/upload/page.tsx"
      provides: "Upload page with shared NavHeader"
      contains: "NavHeader"
  key_links:
    - from: "app/plan/_components/CalendarView.tsx"
      to: "/study"
      via: "Link href on exam/reading task cards in DayView"
      pattern: "study\\?task_id="
    - from: "app/syllabi/upload/page.tsx"
      to: "app/plan/_components/NavHeader.tsx"
      via: "NavHeader component import"
      pattern: "import.*NavHeader"
---

<objective>
Close all v1.0 milestone audit gaps: add study session entry points to CalendarView DayView, replace upload page custom header with shared NavHeader, remove dead code (PlanGrid.tsx, DayTimeline.tsx, stale revalidatePath calls), and update REQUIREMENTS.md to reflect actual completion status.

Purpose: Ship a clean v1.0 with no orphaned integration points, no dead code, and accurate documentation.
Output: All audit gaps closed, dead code removed, REQUIREMENTS.md fully up to date.
</objective>

<execution_context>
@/Users/trevorperkes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trevorperkes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@app/plan/_components/CalendarView.tsx
@app/plan/_components/DayTimeline.tsx
@app/syllabi/upload/page.tsx
@app/plan/actions.ts
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix integration gaps and remove dead code</name>
  <files>
    app/plan/_components/CalendarView.tsx
    app/syllabi/upload/page.tsx
    app/plan/_components/PlanGrid.tsx
    app/plan/_components/DayTimeline.tsx
    app/plan/actions.ts
  </files>
  <action>
    **1. Add study links to CalendarView DayView (STDY-02):**

    In `CalendarView.tsx`, add `import Link from "next/link"` to the imports.

    In the `TaskCard` component (around line 240-284), add a Study link for exam/reading tasks. After the status badge span (line ~273) and before the edit icon SVG (line ~275), add:

    ```tsx
    {(task.type === "exam" || task.type === "reading") && (
      <Link
        href={`/study?task_id=${task.id}`}
        className="rounded px-1.5 py-0.5 text-[10px] font-medium text-blue-600 hover:bg-blue-50 hover:text-blue-800"
        onClick={(e) => e.stopPropagation()}
      >
        Study
      </Link>
    )}
    ```

    The `onClick stopPropagation` is critical — TaskCard is wrapped in a button with an onClick handler; without it, clicking Study would also trigger the task edit modal.

    **2. Replace upload page custom header with NavHeader (VIEW-01):**

    In `app/syllabi/upload/page.tsx`:
    - Add `import NavHeader from "@/app/plan/_components/NavHeader"` (same import path used by dashboard, tasks, courses, settings pages)
    - Replace the entire `<header>...</header>` block (lines 38-53) with `<NavHeader />`
    - Remove the `import Link from "next/link"` at the top ONLY if the remaining code no longer uses Link. Check: the bottom of the page still has a Link ("Prefer to add tasks manually?"), so keep the Link import.

    **3. Remove dead code:**

    - Delete `app/plan/_components/PlanGrid.tsx` entirely (175 lines, never imported)
    - Delete `app/plan/_components/DayTimeline.tsx` entirely (never imported after CalendarView replaced it)
    - In `app/plan/actions.ts`, remove all 4 instances of `revalidatePath("/plan/day")` (lines 164, 200, 227, 283). The `/plan/day` route is a redirect, not a data page. Keep the adjacent `revalidatePath("/plan")` and `revalidatePath("/dashboard")` calls.

    **Do NOT** remove any other revalidatePath calls — only the "/plan/day" ones are stale.
  </action>
  <verify>
    <automated>cd "/Users/trevorperkes/Downloads/BYUI Student Program" && npx next build 2>&1 | tail -20</automated>
    <manual>Verify: (1) CalendarView.tsx contains "/study?task_id=" link, (2) upload page imports NavHeader, (3) PlanGrid.tsx and DayTimeline.tsx are gone, (4) no revalidatePath("/plan/day") in actions.ts</manual>
  </verify>
  <done>
    CalendarView DayView shows Study link on exam/reading task cards. Upload page uses NavHeader. PlanGrid.tsx and DayTimeline.tsx deleted. All revalidatePath("/plan/day") calls removed. Build succeeds with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Update `.planning/REQUIREMENTS.md` to reflect actual completion status per the audit:

    **1. Check checkboxes for satisfied requirements (already implemented but unchecked):**
    These are already `[x]` in the current file — confirmed by reading. No checkbox changes needed for these.

    **2. Update VIEW-01 and STDY-02 status:**
    - `VIEW-01`: Change from `[ ]` to `[x]` (will be fixed by Task 1)
    - `STDY-02`: Change from `[ ]` to `[x]` (will be fixed by Task 1)

    **3. Update traceability table:**
    - `VIEW-01`: Change status from `Pending` to `Complete`
    - `STDY-02`: Change status from `Pending` to `Complete`

    **4. Update the "Last updated" line at the bottom:**
    Change to: `*Last updated: 2026-03-05 after Phase 8 integration fixes — all v1 requirements complete*`
  </action>
  <verify>
    <automated>cd "/Users/trevorperkes/Downloads/BYUI Student Program" && grep -c "\[ \]" .planning/REQUIREMENTS.md | grep -q "^0$" && echo "All v1 checkboxes checked" || echo "FAIL: unchecked boxes remain"</automated>
    <manual>Verify no v1 requirement has unchecked checkbox, traceability table shows all Complete</manual>
  </verify>
  <done>
    All 45 v1 requirements show `[x]` checked. Traceability table shows all 45 as Complete. No Pending statuses remain in v1 section.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with no errors
2. `grep -r "PlanGrid\|DayTimeline" app/` returns no results (dead code removed, no remaining imports)
3. `grep -c "revalidatePath.*plan/day" app/plan/actions.ts` returns 0
4. `grep "study?task_id=" app/plan/_components/CalendarView.tsx` returns matches (study links present)
5. `grep "NavHeader" app/syllabi/upload/page.tsx` returns match (shared nav used)
6. `grep -c "\[ \]" .planning/REQUIREMENTS.md` in v1 section returns 0 (all checked)
</verification>

<success_criteria>
- CalendarView DayView displays "Study" link on exam and reading task cards that navigates to /study?task_id={id}
- Upload page renders shared NavHeader with full navigation
- PlanGrid.tsx and DayTimeline.tsx no longer exist in the codebase
- No stale revalidatePath("/plan/day") calls remain
- All 45 v1 requirements checked and marked Complete in traceability
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-fixes/08-01-SUMMARY.md`
</output>
