---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - app/settings/page.tsx
  - app/settings/actions.ts
  - components/delete-account-form.tsx
autonomous: true
requirements: [AUTH-05]
must_haves:
  truths:
    - "User can delete their account from settings page"
    - "User must type DELETE to confirm account deletion"
    - "After deletion, all user data is removed (cascade delete)"
    - "After deletion, user is redirected to /auth with confirmation message"
    - "Storage objects are deleted before user account removal"
  artifacts:
    - path: "app/settings/page.tsx"
      provides: "Settings page with account deletion"
    - path: "app/settings/actions.ts"
      provides: "Server Action for account deletion using admin client"
      exports: ["deleteAccount"]
    - path: "components/delete-account-form.tsx"
      provides: "Delete confirmation form requiring DELETE text input"
  key_links:
    - from: "app/settings/actions.ts"
      to: "lib/supabase/admin.ts"
      via: "admin.auth.admin.deleteUser()"
      pattern: "deleteUser"
    - from: "app/settings/actions.ts"
      to: "storage.objects"
      via: "delete user's storage files before account deletion"
      pattern: "storage.*remove"
    - from: "components/delete-account-form.tsx"
      to: "app/settings/actions.ts"
      via: "form action for deleteAccount"
      pattern: "deleteAccount"
---

<objective>
Implement account deletion with safety confirmation, storage cleanup, and cascade data removal.

Purpose: Complete the auth lifecycle — users have full control over their data, including permanent deletion (AUTH-05).
Output: Settings page with account deletion requiring "DELETE" confirmation, cascade data removal, storage cleanup.
</objective>

<execution_context>
@/Users/trevorperkes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trevorperkes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings page with account deletion</name>
  <files>
    app/settings/page.tsx
    app/settings/actions.ts
    components/delete-account-form.tsx
  </files>
  <action>
    1. Create `components/delete-account-form.tsx` — Client Component:
       - Text input where user must type "DELETE" exactly to enable the delete button
       - Delete button is disabled until input matches "DELETE"
       - Button styled in red (destructive action: bg-red-600, hover:bg-red-700)
       - Calls `deleteAccount` Server Action on submit
       - Shows loading state during deletion ("Deleting account..." with disabled button)
       - Shows error message inline if deletion fails

    2. Create `app/settings/actions.ts` — Server Actions ("use server"):
       - `deleteAccount(formData: FormData)`:
         a. Get current user via `supabase.auth.getUser()` (server client)
         b. Validate confirmation text from formData equals "DELETE"
         c. List and delete all storage objects in `syllabi/{user_id}/` folder:
            - Use admin client: `supabase.storage.from('syllabi').list(user_id)`
            - Delete all files: `supabase.storage.from('syllabi').remove([...paths])`
         d. Delete user via admin client: `adminClient.auth.admin.deleteUser(user.id)`
         e. Sign out current session: `supabase.auth.signOut()`
         f. Redirect to `/auth?message=deleted`
         g. On any error: redirect to `/settings?error={message}`
       - Import admin client from `lib/supabase/admin.ts`
       - Import server client from `lib/supabase/server.ts`

    3. Create `app/settings/page.tsx` — Server Component:
       - Auth check: get user, redirect to `/auth` if not authenticated
       - Page title: "Settings"
       - Section: "Delete Account" with warning text explaining the action is permanent
       - Warning: "This will permanently delete your account and all associated data including terms, courses, tasks, and uploaded syllabi. This action cannot be undone."
       - Include DeleteAccountForm component
       - Show error from URL params if present
       - Include navigation back to dashboard
       - Include SignOutButton in header (same pattern as dashboard/onboarding)

    Add a link to `/settings` in the dashboard and onboarding header navigation.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <manual>Build succeeds. Settings page exists with delete account form. Dashboard has link to settings.</manual>
  </verify>
  <done>Settings page renders with account deletion form. User must type "DELETE" to enable the button. Deletion removes storage objects first, then deletes user account (triggering cascade delete on all tables), signs out, and redirects to /auth with confirmation message.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Settings page accessible at `/settings`
- Delete account form requires "DELETE" confirmation text
- Server Action uses admin client for deletion
- Storage cleanup happens before user deletion
- Redirect to `/auth?message=deleted` after successful deletion
- Navigation links between dashboard, onboarding, and settings work
</verification>

<success_criteria>
- User can navigate to settings from any authenticated page
- User must type "DELETE" to confirm (prevents accidental deletion per CONTEXT.md)
- Account deletion removes storage objects first (Supabase requirement)
- Account deletion triggers cascade delete on all tables (via FK constraints in migration)
- After deletion, user is signed out and redirected to /auth with "Account deleted" message
- No grace period, no soft delete (per CONTEXT.md: instant deletion)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
