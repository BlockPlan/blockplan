---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, SECU-01, SECU-02, SECU-03]
must_haves:
  truths:
    - "User can create an account with email and password and land on their dashboard"
    - "User can sign in with email and password and remain signed in across browser refreshes"
    - "User can sign out from any page and is returned to the login screen"
    - "User can delete their account and all associated data is removed"
    - "No user can access another user's data — RLS blocks cross-user queries on all tables"
  artifacts: []
  key_links: []
---

<objective>
Apply the database migration and verify the complete Phase 1 auth flow end-to-end.

Purpose: Ensure all foundation pieces work together — auth flows, route protection, database schema, RLS, and account deletion.
Output: Verified working authentication system with database schema applied and RLS enforced.
</objective>

<execution_context>
@/Users/trevorperkes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trevorperkes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply database migration and configure Supabase</name>
  <action>
    The user must apply the database migration to their Supabase project. This requires Supabase Dashboard access which Claude cannot perform.
  </action>
  <how-to-verify>
    1. Create a Supabase project at https://supabase.com/dashboard if you haven't already
    2. Copy your project URL, anon key, and service_role key into `.env.local` (use `.env.local.example` as template)
    3. Go to Supabase Dashboard -> SQL Editor
    4. Paste the contents of `supabase/migrations/00001_initial_schema.sql` and run it
    5. Verify tables created: Dashboard -> Table Editor should show: terms, courses, tasks, subtasks, availability_rules, plan_blocks
    6. Verify RLS enabled: Each table should show a shield icon indicating RLS is on
    7. Go to Authentication -> Providers -> Email -> Turn OFF "Confirm email" (for MVP simplicity)
    8. Type "done" when complete
  </how-to-verify>
  <resume-signal>Type "done" when migration is applied and env vars are set</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 auth flow end-to-end</name>
  <action>Human verifies complete Phase 1 authentication system: sign-up, sign-in, session persistence, sign-out, account deletion, database schema with RLS, and storage bucket</action>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Test 1: Sign Up (AUTH-01)**
    1. Visit http://localhost:3000/auth
    2. Click "Sign Up" tab
    3. Enter a test email and password (min 6 chars)
    4. Click Sign Up
    5. Expected: Redirected to /onboarding with "Onboarding coming in Phase 2" message

    **Test 2: Sign Out (AUTH-04)**
    6. Click "Sign Out" button in the header
    7. Expected: Redirected to /auth

    **Test 3: Sign In (AUTH-02)**
    8. On /auth, ensure "Sign In" tab is active
    9. Enter the same email and password
    10. Click Sign In
    11. Expected: Redirected to /dashboard with "Dashboard coming in Phase 5" message

    **Test 4: Session Persistence (AUTH-03)**
    12. Refresh the browser (F5 or Cmd+R)
    13. Expected: Still on /dashboard, still signed in (not redirected to /auth)

    **Test 5: Route Protection**
    14. While signed in, try navigating to /auth directly
    15. Expected: Redirected to /dashboard (authenticated users can't access /auth)

    **Test 6: Account Deletion (AUTH-05)**
    16. Navigate to /settings
    17. Type "DELETE" in the confirmation input
    18. Click the delete button
    19. Expected: Redirected to /auth with "Account deleted" message
    20. Try signing in with the deleted account's credentials
    21. Expected: Error message (account no longer exists)

    **Test 7: RLS Verification (SECU-02)**
    22. In Supabase Dashboard -> SQL Editor, run:
        `SELECT * FROM public.terms;` (using service role — should show all rows)
    23. From the app (using anon key via client), any query should only return the current user's rows

    Report: Type "approved" if all tests pass, or describe what failed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria verified through manual testing:
1. Sign up with email/password -> lands on dashboard/onboarding
2. Sign in with email/password -> remains signed in across refreshes
3. Sign out from any page -> returns to login
4. Account deletion -> all data removed
5. RLS enforced on all tables
</verification>

<success_criteria>
- Database migration applied successfully
- All 6 tables visible in Supabase Dashboard with RLS enabled
- Complete auth flow works end-to-end
- Session persists across browser refresh
- Account deletion cascade removes all data
- Route protection enforced by middleware
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
