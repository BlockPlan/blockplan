---
phase: 05-views-and-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/plan/_components/NavHeader.tsx
  - app/plan/page.tsx
  - app/plan/actions.ts
  - app/dashboard/page.tsx
  - app/tasks/page.tsx
  - app/courses/page.tsx
  - app/settings/page.tsx
autonomous: true
requirements: [VIEW-01, VIEW-04]

must_haves:
  truths:
    - "Weekly view shows scheduled blocks per day with the task assigned to each block"
    - "User can mark blocks as done or missed from the weekly view"
    - "All authenticated pages share a consistent navigation header with active state indicator"
  artifacts:
    - path: "app/plan/_components/NavHeader.tsx"
      provides: "Shared navigation with active link detection"
      min_lines: 20
    - path: "app/plan/page.tsx"
      provides: "Enhanced weekly view with navigation"
  key_links:
    - from: "app/plan/_components/NavHeader.tsx"
      to: "next/navigation"
      via: "usePathname for active state"
      pattern: "usePathname"
    - from: "app/plan/page.tsx"
      to: "app/plan/_components/PlanGrid.tsx"
      via: "component import"
      pattern: "import PlanGrid"
---

<objective>
Extract a shared navigation component and integrate it across all authenticated pages. Enhance the weekly plan view with proper navigation and ensure VIEW-01 requirements are met.

Purpose: Establish consistent navigation across the app and confirm the weekly view (already mostly built in Phase 4) satisfies VIEW-01 and VIEW-04.
Output: NavHeader component used by all authenticated pages; enhanced /plan page.
</objective>

<execution_context>
@/Users/trevorperkes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trevorperkes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-views-and-dashboard/05-RESEARCH.md
@app/plan/page.tsx
@app/plan/_components/PlanGrid.tsx
@app/plan/_components/PlanBlock.tsx
@app/dashboard/page.tsx
@app/tasks/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared NavHeader component and integrate into all pages</name>
  <files>
    app/plan/_components/NavHeader.tsx
    app/plan/page.tsx
    app/dashboard/page.tsx
    app/tasks/page.tsx
    app/courses/page.tsx
    app/settings/page.tsx
  </files>
  <action>
    Create a client component `NavHeader.tsx` in `app/plan/_components/` that provides consistent navigation across all authenticated pages.

    NavHeader requirements:
    - "use client" directive (needs usePathname)
    - Import `usePathname` from `next/navigation`, `Link` from `next/link`, and `SignOutButton` from `@/components/sign-out-button`
    - Layout: border-b border-gray-200 bg-white header with mx-auto max-w-5xl px-4 py-3 inner container
    - Left side: "BlockPlan" text as h1 (text-lg font-semibold text-gray-900) linked to /dashboard
    - Right side: nav links + SignOutButton
    - Navigation links: Dashboard (/dashboard), Plan (/plan), Tasks (/tasks), Courses (/courses), Settings (/settings)
    - Active state: use `usePathname()` — if pathname === href or pathname.startsWith(href + "/"), show font-medium text-gray-900; otherwise text-sm text-gray-500 hover:text-gray-700
    - Export as default

    Then update ALL authenticated pages to use NavHeader instead of their inline headers:

    **app/plan/page.tsx:** Replace the entire `<header>...</header>` block with `<NavHeader />`. Remove the inline Link and SignOutButton imports that are no longer needed (keep the other imports). The NavHeader import path is `./_components/NavHeader` since it's in the plan directory but shared.

    **app/dashboard/page.tsx:** Replace the inline header with `import NavHeader from "@/app/plan/_components/NavHeader"` and use `<NavHeader />`. Remove SignOutButton and Link imports if no longer needed.

    **app/tasks/page.tsx:** Replace the inline header with `import NavHeader from "@/app/plan/_components/NavHeader"` and use `<NavHeader />`. Remove SignOutButton and Link imports if no longer needed.

    **app/courses/page.tsx:** Replace the inline header with `import NavHeader from "@/app/plan/_components/NavHeader"` and use `<NavHeader />`. Remove SignOutButton and Link imports if no longer needed.

    **app/settings/page.tsx:** Replace the inline header with `import NavHeader from "@/app/plan/_components/NavHeader"` and use `<NavHeader />`. Remove SignOutButton and Link imports if no longer needed.

    Keep the same visual style as existing headers (border-b, bg-white, max-w-5xl).
  </action>
  <verify>Run `npx next build` — should compile with no errors. Verify NavHeader.tsx exists and all 5 pages import it.</verify>
  <done>All authenticated pages show the same NavHeader with Dashboard, Plan, Tasks, Courses, Settings links and active state indicator. No duplicate header code in any page.</done>
</task>

<task type="auto">
  <name>Task 2: Update revalidation paths in Server Actions for cross-view consistency</name>
  <files>
    app/plan/actions.ts
  </files>
  <action>
    Update the existing Server Actions in `app/plan/actions.ts` to revalidate additional paths so that marking a block done/missed from any view updates all views.

    In `generatePlan()`: Add `revalidatePath("/dashboard")` and `revalidatePath("/plan/day")` alongside the existing `revalidatePath("/plan")`.

    In `markBlockDone()`: Add `revalidatePath("/dashboard")` and `revalidatePath("/plan/day")` alongside the existing `revalidatePath("/plan")`.

    In `markBlockMissed()`: Add `revalidatePath("/dashboard")` and `revalidatePath("/plan/day")` alongside the existing `revalidatePath("/plan")`.

    This ensures that when a user marks a block done or missed from the weekly view (or when a new plan is generated), the daily view and dashboard will also show updated data on next navigation.
  </action>
  <verify>Run `npx next build` — should compile with no errors. Grep for `revalidatePath("/dashboard")` in actions.ts — should appear 3 times.</verify>
  <done>All three Server Actions revalidate /plan, /plan/day, and /dashboard paths. Cross-view data consistency is maintained.</done>
</task>

</tasks>

<verification>
- NavHeader component renders with correct links and active state detection
- All authenticated pages (plan, dashboard, tasks, courses, settings) use NavHeader
- The /plan page still shows the 7-day PlanGrid with blocks, done/missed buttons, risk badges
- Server Actions revalidate all three view paths
- `npx next build` succeeds with no errors
</verification>

<success_criteria>
- Consistent navigation across all authenticated pages
- Weekly view at /plan shows blocks per day with task titles, time ranges, course names (VIEW-01)
- Users can mark blocks done or missed from the weekly view (VIEW-04)
- Active state indicator highlights current page in navigation
</success_criteria>

<output>
After completion, create `.planning/phases/05-views-and-dashboard/05-01-SUMMARY.md`
</output>
