---
phase: 07-export-and-polish
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - app/plan/_components/NavHeader.tsx
  - app/plan/_components/PlanGrid.tsx
  - app/plan/_components/PlanBlock.tsx
  - app/plan/day/page.tsx
  - app/plan/_components/DayTimeline.tsx
  - app/dashboard/_components/DashboardContent.tsx
  - app/tasks/_components/TaskForm.tsx
  - app/tasks/_components/TaskList.tsx
  - app/tasks/_components/TaskFilters.tsx
  - app/onboarding/_components/WizardShell.tsx
  - app/onboarding/_components/StepAvailability.tsx
  - app/settings/_components/PlannerSettings.tsx
  - app/study/_components/StudySession.tsx
  - app/syllabi/upload/_components/UploadForm.tsx
  - app/syllabi/review/_components/ReviewScreen.tsx
autonomous: true
requirements: [RESP-01]
must_haves:
  truths:
    - "All screens are usable on a 375px-wide mobile screen"
    - "No horizontal scroll on any page at 375px"
    - "No overlapping elements on any page at 375px"
    - "Navigation is accessible via hamburger menu on mobile"
    - "Plan grid shows single-column scrollable layout on mobile"
    - "Forms stack vertically on narrow screens"
  artifacts:
    - path: "app/plan/_components/NavHeader.tsx"
      provides: "Hamburger menu for mobile navigation"
  key_links:
    - from: "app/plan/_components/NavHeader.tsx"
      to: "All authenticated pages"
      via: "Shared import"
      pattern: "NavHeader"
---

<objective>
Audit and fix all screens for mobile responsiveness at 375px width. Add hamburger navigation for mobile, fix plan grid to single-column on mobile, and ensure all forms and content stack properly on narrow screens.

Purpose: The app must be usable on phones. This is the final polish step before the app is shippable.
Output: All pages responsive at 375px+ with no horizontal scroll or overlapping elements.
</objective>

<execution_context>
@/Users/trevorperkes/.claude/get-shit-done/workflows/execute-plan.md
@/Users/trevorperkes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/plan/_components/NavHeader.tsx
@app/plan/_components/PlanGrid.tsx
@app/plan/_components/PlanBlock.tsx
@app/plan/day/page.tsx
@app/dashboard/_components/DashboardContent.tsx
@app/tasks/_components/TaskForm.tsx
@app/tasks/_components/TaskList.tsx
@app/onboarding/_components/WizardShell.tsx
@app/settings/_components/PlannerSettings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hamburger menu to NavHeader for mobile</name>
  <files>app/plan/_components/NavHeader.tsx</files>
  <action>
Refactor NavHeader.tsx to support mobile hamburger menu:

1. Add "use client" (already present) and import useState from react
2. Add mobileOpen state: const [mobileOpen, setMobileOpen] = useState(false)
3. Desktop nav (existing links) — add `hidden sm:flex` to the nav element:
   ```tsx
   <nav className="hidden sm:flex items-center gap-4">
   ```
4. Add hamburger button visible only on mobile:
   ```tsx
   <button
     className="sm:hidden p-2 text-gray-600 hover:text-gray-900"
     onClick={() => setMobileOpen(!mobileOpen)}
     aria-label="Toggle menu"
   >
     <svg className="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
       {mobileOpen ? (
         <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" />
       ) : (
         <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
       )}
     </svg>
   </button>
   ```
5. Add mobile dropdown menu below header:
   ```tsx
   {mobileOpen && (
     <nav className="border-t border-gray-100 bg-white px-4 pb-3 pt-2 sm:hidden">
       <div className="flex flex-col gap-2">
         {/* Same NavLink components as desktop, rendered vertically */}
         {/* Each link also calls setMobileOpen(false) on click */}
       </div>
       <div className="mt-2 border-t border-gray-100 pt-2">
         <SignOutButton />
       </div>
     </nav>
   )}
   ```
6. Make the header wrapper `relative` so mobile menu can position correctly
7. Close mobile menu when a NavLink is clicked — modify NavLink to accept optional onClick prop, or wrap in a parent that handles click
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors. Verify NavHeader.tsx has hamburger button and mobile menu.
  </verify>
  <done>NavHeader shows hamburger icon on mobile (< 640px) that toggles a vertical dropdown menu. Desktop nav is unchanged. Menu closes when a link is clicked.</done>
</task>

<task type="auto">
  <name>Task 2: Fix responsive issues across all screens</name>
  <files>app/plan/_components/PlanGrid.tsx, app/plan/_components/PlanBlock.tsx, app/plan/day/page.tsx, app/plan/_components/DayTimeline.tsx, app/dashboard/_components/DashboardContent.tsx, app/tasks/_components/TaskForm.tsx, app/tasks/_components/TaskList.tsx, app/tasks/_components/TaskFilters.tsx, app/onboarding/_components/WizardShell.tsx, app/onboarding/_components/StepAvailability.tsx, app/settings/_components/PlannerSettings.tsx, app/study/_components/StudySession.tsx, app/syllabi/upload/_components/UploadForm.tsx, app/syllabi/review/_components/ReviewScreen.tsx</files>
  <action>
Audit and fix each component for 375px mobile. Key changes by component:

**PlanGrid.tsx:**
- Change `grid grid-cols-1 sm:grid-cols-7` to `grid grid-cols-1 md:grid-cols-7` (7 columns at 768px+, not 640px — 7 cols at 640px makes each column ~80px which is unreadable)
- Header row (title + buttons): change to `flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between` so buttons stack below title on narrow screens
- Ensure export button + generate button wrap properly on mobile

**PlanBlock.tsx:**
- Ensure text doesn't overflow: add `truncate` or `break-words` to title text
- Minimum touch target: ensure block is at least 44px tall (min-h-[44px])

**DayTimeline.tsx (app/plan/_components/):**
- Ensure timeline items stack vertically and don't overflow
- Time labels: shrink font or abbreviate on small screens if needed

**DashboardContent.tsx:**
- Cards grid: verify `grid-cols-1 md:grid-cols-2` is in place (already may be correct based on history digest)
- Quick action links: ensure they wrap with `flex-wrap` if needed
- Progress bar: ensure it doesn't overflow container

**TaskForm.tsx:**
- All form fields should be full width on mobile (w-full by default, no fixed widths)
- Input fields must use `text-base` (16px) to prevent iOS zoom on focus
- Submit button full width on mobile: `w-full sm:w-auto`

**TaskList.tsx:**
- Table rows or cards should be readable at 375px
- If currently a table layout, consider cards on mobile or horizontal scroll on the table only (not the page)
- Action buttons (edit, delete, study) — ensure they don't overflow the row. Stack vertically or use icon-only on mobile.
- Add `overflow-x-auto` wrapper around any table elements

**TaskFilters.tsx:**
- Filter controls should stack vertically on mobile: `flex flex-col gap-2 sm:flex-row`

**WizardShell.tsx:**
- Step indicators should be responsive — abbreviate or use numbers-only on mobile
- Main content area: ensure padding doesn't waste space (px-4 not px-8 on mobile)

**StepAvailability.tsx / AvailabilityGrid.tsx:**
- The availability grid is inherently wide (7 days x many time slots). Wrap in overflow-x-auto container so it scrolls horizontally within the page (the grid scrolls, NOT the page)
- Add a hint text on mobile: "Scroll right to see all days"

**PlannerSettings.tsx:**
- Form inputs full width on mobile, stack vertically
- Input text-base to prevent iOS zoom

**StudySession.tsx:**
- Content area full width, no fixed widths
- Study aids (summary, terms, questions) should stack and be readable

**UploadForm.tsx:**
- File input and course selector full width
- Progress bar contained within card

**ReviewScreen.tsx:**
- Parsed items should be readable as cards at 375px
- Edit form fields stack vertically
- Action buttons don't overflow

General rules applied to ALL components:
- No fixed px widths on containers (use max-w-* + w-full)
- All text inputs use `text-base` class (prevents iOS zoom, 16px minimum)
- Touch targets minimum 44px (min-h-[44px] or p-3 on buttons)
- Use `overflow-hidden` or `overflow-x-auto` where content might overflow
- Prefer `px-4` padding on mobile (not px-6 or px-8)
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors. Run `npm run build` to verify the build succeeds.
  </verify>
  <done>All screens are usable at 375px width. No horizontal page scroll. No overlapping elements. Forms stack vertically. Plan grid shows single column on mobile. Availability grid scrolls horizontally within its container. Touch targets are at least 44px.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] NavHeader shows hamburger on mobile, full nav on desktop
- [ ] PlanGrid single-column on mobile, 7-column on desktop
- [ ] No horizontal page scroll at 375px on any route
- [ ] All form inputs use text-base (no iOS zoom)
- [ ] Touch targets at least 44px on interactive elements
</verification>

<success_criteria>
All screens are usable on a 375px-wide mobile screen without horizontal scroll or overlapping elements. Navigation uses hamburger menu on mobile. Plan grid is single-column scrollable. Forms stack vertically.
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-and-polish/07-03-SUMMARY.md`
</output>
